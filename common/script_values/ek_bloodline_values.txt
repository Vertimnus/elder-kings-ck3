trait_counter = {
    value = 0
    # if = {
    #     limit = { has_trait = bloodline_test_1 }
    #     add = 1
    # }
    # if = {
    #     limit = { has_trait = bloodline_test_2 }
    #     add = 1
    # }
    # if = {
    #     limit = { has_trait = bloodline_test_3 }
    #     add = 1
    # }
    if = {
        limit = { has_trait = bloodline_direnni }
        add = 1
    }
    if = {
        limit = { has_trait = bloodline_reman }
        add = 1
    }
}