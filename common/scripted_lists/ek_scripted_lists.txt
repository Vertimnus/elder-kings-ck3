# Bloodlines
bloodline_character = {
	base = living_character
	conditions = { has_trait_with_flag = bloodline }
}

# NOT birthsign character

non_birthsign = {
	base = living_character
	conditions = { NOT = { has_trait = birthsign } }
}